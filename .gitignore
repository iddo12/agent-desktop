node_modules/
electron_pid.txt

# Local Claude Code dev tooling (the run-agent-desktop driver skill, etc.) -
# useful for developing this app, not part of the app itself.
.claude/

# Author's own internal dev notes/changelog - full of environment-specific
# history that isn't useful (and isn't meant) as public documentation.
CLAUDE.md

# Sidebar groups are per-install, user-specific config (which agents sit in
# which visual group). Normally this file lives next to the agent folders,
# outside the repo - but if someone points AGENT_DESKTOP_ROOT at the repo
# itself, ignore it here so their own grouping never gets committed.
agent_groups.json
